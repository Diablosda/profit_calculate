# 2026-08-11 工作记录

- 根据 `月度核算_2026.ipynb` 与 `matching_tools_new.py` 的逻辑，为 MG 账号构建了月度核算 Notebook：`E:/核算相关/核算数据分析/MG月度核算.ipynb`。
- Notebook 输入目录为 `MG/{年月}/报表/`，输出中间表与结果表到 `MG/{年月}/核算结果/`。
- 默认启用 US/CA 市场（属性表当前仅维护这两个国家），并预留 MX/BR 配置与列名标准化映射。
- 已通过 nbconvert 执行验证：空报表时跳过无数据输出，不报错；放入示例交易报表后可正常生成中间表与 MSKU 绩效明细。
